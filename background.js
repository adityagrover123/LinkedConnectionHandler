chrome.action.onClicked.addListener((tab) => {
    if (tab.url.includes("linkedin.com/mynetwork/grow")) {
        chrome.scripting.executeScript({
            target: { tabId: tab.id },
            func: logConnectButtons
        });
    } else {
        console.log("Not a LinkedIn 'My Network' page.");
    }
});
chrome.action.onClicked.addListener((tab) => {
    if (tab.url.includes("linkedin.com/mynetwork/grow")) {
        chrome.scripting.executeScript({
            target: { tabId: tab.id },
            func: logConnectButtons
        });
    } else {
        console.log("Not a LinkedIn 'My Network' page.");
    }
});
chrome.action.onClicked.addListener((tab) => {
    if (tab.url.includes("linkedin.com/mynetwork/grow")) {
        chrome.scripting.executeScript({
            target: { tabId: tab.id },
            func: logConnectButtons
        });
    } else {
        console.log("Not a LinkedIn 'My Network' page.");
    }
});
 chrome.action.onClicked.addListener((tab) => {
    if (tab.url.includes("linkedin.com/mynetwork/grow")) {
        chrome.scripting.executeScript({
            target: { tabId: tab.id },
            func: logConnectButtons
        });
    } else {
        console.log("Not a LinkedIn 'My Network' page.");
    }
});